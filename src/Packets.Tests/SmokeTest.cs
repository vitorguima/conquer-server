using Xunit;

namespace Conquer.Packets.Tests
{
    /// <summary>Trivial smoke test so the runner discovers the Packets.Tests project.</summary>
    public class SmokeTest
    {
        [Fact]
        public void ProjectDiscovered()
        {
            Assert.True(true);
        }
    }
}
