# Codebase & Environment Analysis

## Existing Code

**Directory structure is greenfield:**
```
C:\Users\Windows\conquer-server/
├── .claude/
│   └── settings.local.json
└── specs/
    └── _epics/
        └── conquer-online-server/
            └── .progress.md
```

- No source code, no git repo, no build files
- No game assets (maps, monsters, items, database files)
- No config or .env files

## Tech Stack

No stack established yet. Recommendations ranked:

| Rank | Language | Reason |
|------|----------|--------|
| 1 | C# / .NET Core 8+ | Industry standard for CO emus; cross-platform; strong async networking |
| 2 | Java (Netty) | Cross-platform, battle-tested async networking |
| 3 | Go | Simple cross-compilation, single binary deploy |
| 4 | C++ | Max performance but long dev cycle |

## Assets Found

None. Assets needed:
- Map data (DAT/DMA files from CO client)
- Item/NPC/monster database dumps
- CO TCP protocol definition
- Reference implementation (Redux or similar)

## Environment Constraints

**Local (dev):** Windows 11 Pro x86-64, PowerShell 5.1, no existing Docker or build tools detected.

**Target (deploy):** Brazilian VPS, Linux x86-64.

**Cross-platform strategy:**
- .NET Core: `dotnet publish -r linux-x64` or Docker
- Go: native cross-compilation
- Test locally via WSL2 or Docker before deploy

## Recommendations

1. Initialize git immediately
2. Choose C# .NET Core 8 — largest existing CO emulator reference pool
3. Create project structure: `src/`, `tests/`, `config/`, `data/`
4. Source Redux or COEmu as the reference/fork base
5. Use Docker for local Linux parity testing before VPS deploy
